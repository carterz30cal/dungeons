package com.carterz30cal.mobs;


/*
 * PHYSICAL = take damage normally
 * MAGIC = unaffected by armour
 * TRUE = removes health directly
 * PERCENT = removes a percent
 */
public enum DamageType
{
	PHYSICAL,
	MAGIC,
	TRUE,
	PERCENT
}
